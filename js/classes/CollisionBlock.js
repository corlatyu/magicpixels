//
// Kelas CollisionBlock, lebih untuk debug
//
class CollisionBlock {
  constructor({ position }) {
    this.position = position;
    this.width = 16;
    this.height = 16;
  }

  draw() {
    // Metode draw akan diimplementasikan untuk debug, tetapi tidak digunakan dalam permainan
  }
}
